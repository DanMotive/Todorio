# 2026-07-30

- Подтверждена полная очистка локализаций в коммите `1cfbce0`: удалены stash-conflict markers и дубли ключей, все 13 базовых локалей валидны и содержат одинаковые 649 уникальных ключей.
- Проверки `check_i18n.py`, `i18n-add-keys.mjs --check`, JSON/key parity, отсутствие conflict markers, TypeScript и Vite production build прошли. Единственное предупреждение — существующий JS chunk 704.09 kB.
- Анализ улучшений показал приоритеты: встроить duplicate-key/conflict-marker/placeholder-parity проверки в основной i18n checker и CI; учитывать server-side ключи при анализе unused; добавить тесты всех notification kinds/locales и dev-only наблюдаемость raw-key fallback; затем code splitting фронтенда.
- Проведён read-only аудит багов и создан `outputs/bug-audit-2026-07-30.md`. Главные находки: race первого root, неатомарное погашение invite, возможность admin сбросить пароль root/admin, ложный успех approve/reset/status, устаревшие маршруты legacy AdminPage, неработающий UI password-protected public share, unsafe theme JSON parse, массовое подавление frontend mutation errors и нетранзакционное применение шаблонов. Frontend build прошёл; Go-тесты не запущены из-за отсутствия `go`.
- Реализован пакет исправлений по всем 12 пунктам аудита: атомарные registration/invite, DB integrity migration 0018, проверки ролей и транзакции admin/template/member operations, строгий JSON и надёжная persistence уведомлений, полный history router с readable `/app/*` URL и единым task/note modal layer, password-protected public shares, полные абсолютные share URL, усиленное rich glass, безопасная тема и явные ошибки критичных frontend-мутаций. Удалены активные legacy AdminPage/RefOpener; добавлен SPA deep-link тест.
- Regression pass: TypeScript и Vite build успешны; 13 базовых локалей содержат по 654 ключа, обе i18n-проверки проходят, `git diff --check` чист. Остались ограничения среды: Go отсутствует, поэтому `go test ./...`, `go vet ./...` и применение migration 0018 к PostgreSQL не выполнены. Vite продолжает предупреждать о JS chunk 710.07 kB.
- Обновлены оба частичных IT-slang overlay (`ru-RU-it` и `en-US-it`): добавлены пять строк password-protected public share (`public.open`, `public.password_invalid`, `public.password_placeholder`, `public.password_required`, `public.too_many_attempts`). Оба overlay теперь содержат по 56 совпадающих ключей; JSON/parity, i18n checks, TypeScript, Vite production build и `git diff --check` прошли. Сохранилось неблокирующее предупреждение о JS chunk 710.55 kB.
- Дополнительный аудит 13 языков выявил крупный обход i18n в root-панели: 37 подписей серверных настроек и значения select приходили с backend на английском. UI переведён на динамические `server_setting.<key>`/`server_setting.option.<value>`, добавлено 49 ключей × 13 языков (637 переводов); каждый базовый пакет теперь содержит 703 ключа. Исправлено несовпадение `setting.change` → `audit.action.setting_change`; checker теперь сверяет backend `knownSettings` с локалями. I18n checks, TypeScript, Vite build и `git diff --check` прошли; chunk вырос до 754.20 kB из-за встроенных переводов.
