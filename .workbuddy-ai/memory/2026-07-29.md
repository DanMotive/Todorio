# 2026-07-29

- Исправлен пустой текст центра уведомлений: API теперь формирует локализованное поле `text` из `kind`/`payload`, возвращает `task_id` и сохраняет payload в DTO.
- Клиент переведён на реальный маршрут `POST /api/notifications/read` для одной записи и всех записей; добавлен тип `NotificationItem`.
- Добавлены регрессионные тесты DTO: русский текст, извлечение `task_id`, повреждённый payload и неизвестный тип.
- Проверки TypeScript, Vite production build, i18n и diff прошли; Go-тесты не запускались, поскольку в окружении нет Go/Docker.
- Завершён полный аудит локализации: добавлены переводы portability/share, audit/focus, wallpaper и webhooks во все 13 базовых локалей; locale-наборы теперь содержат по 649 ключей.
- Убраны локальные alias-обёртки, скрывавшие ключи от статического анализа, в sharing/dashboard/insights/members/workflow/wallpaper/webhooks; rich/lite ветки проверены, уникальный lite-текст использует те же локализованные подписи.
- `scripts/check_i18n.py` теперь видит одинарные/двойные/template-строки, template-префиксы, сообщает файл и строку и запрещает неработающий `tr(...) || fallback` и скрывающие его helper-функции.
- Финальные проверки прошли: i18n integrity, i18n key-source check, TypeScript, Vite production build и `git diff --check`; остаются прежние предупреждения CSS-минификатора и размера чанка.
- Исправлена непоследовательность режима `rich`: централизованы glass-токены и на прозрачное стекло переведены все нейтральные поверхности, поля и кнопки — shell, mobile topbar, cards, modal/nested cards, menus, kanban, calendar, timeline и controls. Текстовые и semantic status/accent цвета сохранены.
- `lite` сохранён непрозрачным через те же токены; добавлен `-webkit-backdrop-filter`. Исправлен оборванный комментарий в `theme.css`, поэтому CSS syntax warnings Vite исчезли. TypeScript, Vite и `git diff --check` прошли; остался только warning размера JS-чанка.
